# 주석
# application.properties    스프링 프로젝트의 설정 파일

# 스프링 부트는 내장톰캣( 기본값 존재 : 내장톰캣 포트 8080)
# JSP(이클립스) 서버 동시 실행시 충돌주의
server.port = 8080


# Spring data 스프링이 DB 연결을 관리
# 1. mysql 드라이버
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# 2. DB URL
spring.datasource.url=jdbc:mysql://localhost:3306/springweb
# 3. DB 계정 ID
spring.datasource.username=root
# 4. DB 계정 PASSWORD * 띄어쓰기도 포함
spring.datasource.password=1234

# JPA 관련 설정
# 5. JPA 실행시 자동으로 create    [  DROP -> CREATE ]
spring.jpa.hibernate.ddl-auto=create
# 6. JPA 실행시 자동으로 update    [  존재할 경우 SQL 업데이트 , 없으면 CREATE ]
# spring.jpa.hibernate.ddl-auto=update
# 7. JPA 가 자동으로 작성한 SQL을 console에 출력해주는 명령어
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true

# 로깅 설정
# 1. 기록할 최소 레벨 선택 [ info 이상 레벨만 기록]
logging.level.root = info
# 2. 기록할 파일 경로 [ 경로 / 파일명.log ]
logging.file.name=C:/java/spring.log

# Oauth2 관련된 설정값

# 1. 구글 클라우드 플랫폼에서 신청한 oauth 클라이언트 정보
    # 1. 발급된 구글 클라이언트 id 대입
    # spring.security.oauth2.client.registration.google.client-id =발급된 클라이언트 id
spring.security.oauth2.client.registration.google.client-id =51364952247-koikbej35b2v35vi18t5a1q8d57dm7dj.apps.googleusercontent.com
    # 2. 발급된 구글 클라이언트 pw 대입
spring.security.oauth2.client.registration.google.client-secret =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
    # 3. 요청한 동의항목 [요청할 회원정보들] : 신청한 동의항목 내 명시된 변수/필드명 대입
spring.security.oauth2.client.registration.google.scope = email,profile












